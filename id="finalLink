<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>أدوات واتساب بزنس</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <style>
        body { margin: 0; font-family: 'Segoe UI', sans-serif; background: #f5f7f6; color: #333; }
        
        /* الهيدر لمنع الشكل المقصوص */
        .header { text-align: center; padding: 40px 20px; }
        .header h1 { font-size: 28px; margin: 0; }
        .header span { color: #10b981; }

        .container { display: flex; flex-wrap: wrap; justify-content: center; gap: 25px; padding: 20px; min-height: 400px; }

        .card { 
            background: white; width: 340px; padding: 25px; border-radius: 20px; 
            border: 1px solid #e5e7eb; box-shadow: 0px 10px 30px rgba(0,0,0,0.05); 
            transition: 0.3s; display: flex; flex-direction: column;
        }

        input { width: 100%; padding: 12px; border-radius: 10px; border: 1px solid #ddd; margin-bottom: 12px; box-sizing: border-box; }
        
        button { 
            width: 100%; padding: 12px; border-radius: 10px; border: none; 
            background: #10b981; color: white; font-weight: bold; cursor: pointer; margin-bottom: 8px;
        }
        button:hover { background: #059669; }
        .btn-blue { background: #0ea5e9; }
        .btn-blue:hover { background: #0284c7; }

        #qrDisplay { display: flex; justify-content: center; margin-top: 15px; min-height: 150px; }
        
        /* منطقة النتيجة داخل الكرت */
        .result-area { 
            margin-top: 15px; padding-top: 15px; border-top: 2px dashed #eee; 
            display: none; /* مخفي افتراضياً */
        }

        .features { padding: 60px 20px; text-align: center; background: #fff; margin-top: 40px; }
        .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; max-width: 1100px; margin: auto; }
        .feature { background: #f9fafb; padding: 20px; border-radius: 15px; border: 1px solid #eee; }
    </style>
</head>
<body>

<div class="header">
    <h1>أدوات <span>واتساب</span> الاحترافية</h1>
    <p>أنشئ روابطك وأكواد QR بكل سهولة</p>
</div>

<div class="container" id="container">

    <!-- كرت الرابط -->
    <div class="card" id="linkCard">
        <h2>📲 رابط واتساب</h2>
        <input type="text" id="phone1" placeholder="05XXXXXXXX">
        <input type="text" id="msg1" placeholder="اكتب رسالة (اختياري)">
        <button onclick="genLink()">إنشاء الرابط</button>

        <div id="linkArea" class="result-area">
            <input type="text" id="finalLink" readonly onclick="this.select()" style="background: #f0fdf4; border-color: #10b981;">
            <button onclick="copyLink()">📋 نسخ الرابط</button>
            <button onclick="openLink()" class="btn-blue">🚀 فتح واتساب</button>
        </div>
    </div>

    <!-- كرت QR -->
    <div class="card" id="qrCard">
        <h2>🔳 كود QR</h2>
        <input type="text" id="phone2" placeholder="05XXXXXXXX">
        <input type="text" id="msg2" placeholder="اكتب رسالة (اختياري)">
        <button onclick="genQR()">إنشاء QR</button>
        <div id="qrDisplay"></div>
    </div>

</div>

<!-- قسم الفوائد -->
<div class="features">
    <h2>لماذا تستخدم هذه الأدوات؟</h2>
    <div class="features-grid">
        <div class="feature"><h3>🚀 سرعة الوصول</h3><p>العميل يتواصل معك بضغطة واحدة دون الحاجة لحفظ الرقم.</p></div>
        <div class="feature"><h3>💰 زيادة المبيعات</h3><p>تسهيل عملية التواصل ترفع نسبة التحويل من زائر لمشتري.</p></div>
        <div class="feature"><h3>📱 مظهر احترافي</h3><p>استخدام الـ QR والروابط المختصرة يعطي انطباعاً بالثقة والتنظيم.</p></div>
    </div>
</div>

<script>
    // وظيفة عامة لبناء الرابط
    function getWaLink(phoneId, msgId) {
        let p = document.getElementById(phoneId).value.replace(/\D/g, '');
        let m = document.getElementById(msgId).value;
        if (!p) return null;
        if (p.startsWith('0')) p = '966' + p.substring(1);
        else if (p.length === 9) p = '966' + p;
        return "https://wa.me/" + p + (m ? "?text=" + encodeURIComponent(m) : "");
    }

    function genLink() {
        let link = getWaLink('phone1','msg1');
        if(!link) { alert("يرجى إدخال الرقم"); return; }
        document.getElementById('finalLink').value = link;
        document.getElementById('linkArea').style.display = 'block';
    }

    function copyLink() {
        let linkInput = document.getElementById('finalLink');
        linkInput.select();
        navigator.clipboard.writeText(linkInput.value);
        alert("تم نسخ الرابط بنجاح! ✅");
    }

    function openLink() {
        let link = document.getElementById('finalLink').value;
        if (link) window.open(link, "_blank");
    }

    function genQR() {
        let container = document.getElementById("qrDisplay");
        let link = getWaLink('phone2','msg2');
        if (!link) { alert("يرجى إدخال رقم صحيح"); return; }
        container.innerHTML = "";
        new QRCode(container, { text: link, width: 160, height: 160 });
    }
</script>

</body>
</html>
